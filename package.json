{
  "name": "gamopls-teamcore",
  "private": true,
  "version": "0.0.0",
  "description": "GAMOPLS TeamCore — Human-Machine Teaming Platform monorepo",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "start:all": "node scripts/start-all.mjs",
    "check:architecture": "node scripts/check-architecture-rules.mjs",
    "test:check-architecture": "vitest run scripts/__tests__/check-architecture-rules.test.mjs"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
